# Sacrifice Food Analysis API Configuration
# Copy this file to .env and fill in the values

# API Security - Bearer token for authenticating Unity game requests
# Generate a secure random string, e.g.: openssl rand -hex 32
SACRIFICE_API_KEY=your-secure-api-key-here

# GCP Project Configuration (uses Application Default Credentials)
# No API key needed - billing goes through your GCP project
SACRIFICE_GCP_PROJECT_ID=steam-378309
SACRIFICE_GCP_LOCATION=us-central1

# Optional: Override the model (default: gemini-2.0-flash)
# SACRIFICE_GEMINI_MODEL=gemini-2.0-flash

# Optional: Debug mode (default: false)
# SACRIFICE_DEBUG=true

# For local development, authenticate with:
#   gcloud auth application-default login
#   gcloud config set project steam-378309
